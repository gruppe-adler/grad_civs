name = "Gruppe Adler Civilians"
prefix = "grad_civs"
author = "Gruppe Adler"
template = "cba"
mainprefix = "z"
folder_optionals = true
sig_version = 3

files = [
    "logo_active_ca.paa",
    "logo_ca.paa",
    "overview_co.paa",
    "logo_small_ca.paa",
    "mod.cpp",
    "README.md"
]
include = [
    "./include"
]

key_name = "{{prefix}}_{{version}}"
authority = "{{prefix}}_{{version}}-{{git \"id 8\"}}"

releasebuild = [
    "@zip grad_civs-{{semver.major}}.{{semver.minor}}.{{semver.patch}}"
]

[header_exts]
version= "{{git \"id 8\"}}"
